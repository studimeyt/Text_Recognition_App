<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permission to access the camera -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- Permission to access the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/scanner"
        android:label="@string/app_name"
        android:roundIcon="@drawable/scanner"
        android:supportsRtl="true"
        android:theme="@style/Theme.TextRecognitionApp"
        tools:targetApi="31">
        <activity
            android:name=".signUp"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".LogInSignUp"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".ScannerActivity"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
            <!-- Additional metadata for ScannerActivity -->

        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Specifies the main action of the intent -->

                <category android:name="android.intent.category.LAUNCHER" />
                <!-- The activity appears in the launcher -->
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
            <!-- Additional metadata for MainActivity -->

        </activity>
    </application>

</manifest>